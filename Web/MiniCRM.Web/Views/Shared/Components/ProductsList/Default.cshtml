@model MiniCRM.Web.ViewModels.Products.ProductsListViewModel
@{
    var products = Model.Products.Select(x => new SelectListItem(x.Name, x.Id.ToString()));

}
<select id="productsSelectList" asp-items="products" asp-for="SelectedIDs" multiple>
</select>

